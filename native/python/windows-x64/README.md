Portable CPython 3.12 runtime for local Windows bootstrap.

Source: official CPython NuGet package `python` version `3.12.10`.

This copy is kept in-repo so `helper.bat` and `scripts/start_local_services.py`
can create compatible virtual environments on machines that only have Python 3.13
or no global Python installation at all.
